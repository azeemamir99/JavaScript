'use strict';

// find the bugs

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriverLicense = true;
if (hasDriversLicense) console.log('I can drive');

// const interface = 'audio video'; // 'interface' reserved word
// const private = 991; // 'private' reserved word
// const if= 99; // variable can't be called if